{
  "name": "skill-extractor-algo",
  "version": "1.0.0",
  "description": "Lambda function to extract skills from job postings manually",
  "main": "index.js",
  "scripts": {
    "build": "tsup src",
    "package": "npm run build && node ../../zip.js .",
    "deploy": "npm run package && aws lambda update-function-code --function-name job-skill-extractor-algo --zip-file fileb://lambda.zip"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.902.0",
    "@aws-sdk/client-s3": "^3.901.0",
    "@aws-sdk/lib-dynamodb": "^3.902.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.155",
    "@types/node": "^20.19.19",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  }
}
