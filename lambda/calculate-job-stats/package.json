{
  "name": "calculate-job-stats",
  "version": "1.0.0",
  "description": "AWS Lambda function to calculate job statistics",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "zip": "tsup && rm -rf package-lock.json node_modules && npm install --omit=dev && node ../../zip.js . && npm install"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.902.0",
    "@aws-sdk/lib-dynamodb": "^3.902.0",
    "@neondatabase/serverless": "^1.0.2",
    "@job-market-analyzer/types": "file:../../packages/shared-types",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.3"
  }
}
