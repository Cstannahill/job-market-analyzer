{
  "name": "get-job-postings-paginated-neon",
  "version": "1.0.0",
  "description": "Lambda function that serves paginated job postings from Neon Postgres",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "zip": "tsup && rm -rf package-lock.json node_modules && npm install --omit=dev && node ../../zip.js . && npm install"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.155",
    "@types/node": "^20.19.19",
    "@types/ws": "^8.18.1",
    "tsup": "^8.3.5",
    "typescript": "^5.9.3"
  }
}
