{
  "name": "resume-presigned-url",
  "version": "1.0.0",
  "description": "Lambda function to retrieve presigned URL for S3 upload",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "zip": "tsup && rm -rf package-lock.json node_modules && npm install --omit=dev && node ../../zip.js . && npm install"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.908.0",
    "@aws-sdk/client-s3": "^3.908.0",
    "@aws-sdk/lib-dynamodb": "^3.908.0",
    "@aws-sdk/s3-request-presigner": "^3.908.0",
    "@aws-sdk/util-dynamodb": "^3.913.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.6.2",
    "typescript": "^5.9.3"
  }
}
