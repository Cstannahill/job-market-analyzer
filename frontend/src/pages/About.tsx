import React from 'react';
import { Layout } from '../components/Layout';

const About: React.FC = () => {
    return (
        <Layout>
            <h2>About</h2>
            <p>Job Market Analyzer — a small app to explore job posting trends.</p>
        </Layout>
    );
};

export default About;
